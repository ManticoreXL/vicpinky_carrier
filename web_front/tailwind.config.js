/** @type {import('tailwindcss').Config} */

/*
 * ──────────────────────────────────────────────────────────────────────────
 *  WARM LIGHT THEME  (배경 #F4E7E1 / 포인트 #FF9B45 → #D5451B → #521C0D)
 * ──────────────────────────────────────────────────────────────────────────
 *  스타일 전용 중앙 팔레트. 기능은 건드리지 않고 색만 재정의한다.
 *
 *  핵심 트릭:
 *   - colors.white 를 어두운 브라운 "잉크"(#521C0D)로 리맵 → 기존 다크 테마의
 *     `text-white/90`, `bg-white/[0.05]`, `border-white/[0.07]` 등 글래스/텍스트
 *     유틸이 한 곳에서 라이트 테마로 자동 반전된다.
 *   - colors.orange 스케일을 따뜻한 강조색(#FF9B45/#D5451B/#521C0D)으로 리맵.
 *   - 순수 흰색이 꼭 필요한 곳(컬러 버튼 위 글자 등)은 `text-paper`/`text-[#fff]`로
 *     개별 지정한다.
 */

const INK = '#562F00'; // 다크 브라운 — 본문 텍스트 / 버튼 / 임팩트

module.exports = {
  content: ['./src/**/*.{js,jsx,ts,tsx}'],
  theme: {
    extend: {
      colors: {
        // 기존 white/* 유틸을 라이트 테마용 잉크로 반전
        white: INK,
        // 순수 종이색(크림/흰색)이 필요할 때 명시적으로 사용
        paper: '#FFFFFF',
        // ── 4단 역할 팔레트 ──────────────────────────────
        surface: '#FFFDF1', // 페이지 배경 (아이보리)
        panel:   '#FFCE99', // navbar·큰 div 등 큰 컴포넌트 (피치)
        card:    '#FF9644', // 카드 등 작은 컴포넌트 (오렌지)
        impact:  '#562F00', // 버튼·임팩트·본문 텍스트 (다크 브라운)
        cream: {
          DEFAULT: '#FFFDF1',
          50:  '#FFFDF1',
          100: '#FFF3DC',
          200: '#FFCE99', // 큰 컴포넌트
          300: '#FFB06A',
          400: '#FF9644', // 카드
        },
        ink: {
          DEFAULT: INK,
          soft: '#7A4A12',
          900: '#562F00',
        },
        // 따뜻한 강조 스케일 (orange-* 가 이 값으로 매핑됨)
        orange: {
          50:  '#FFF6EC',
          100: '#FFE7CC',
          200: '#FFCE99', // 피치 (큰 컴포넌트)
          300: '#FFB06A',
          400: '#FF9644', // 오렌지 (카드)
          500: '#F47A22',
          600: '#D5451B',
          700: '#9E3A10',
          800: '#7A2E0A',
          900: '#562F00', // 다크 브라운 (임팩트)
          950: '#3D2100',
        },
      },
    },
  },
  plugins: [],
};
