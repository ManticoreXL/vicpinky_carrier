float32 forward_time      # 전진 시간(초). 0 이하 → 노드 기본값 사용
float32 forward_speed     # 전진 속도(m/s). 0 이하 → 노드 기본값 사용
---
bool    success           # 정상 완료 여부
float32 driven_time       # 실제 전진한 시간(초)
string  message           # 결과 설명
